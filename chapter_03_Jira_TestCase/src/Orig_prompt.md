our main objective is that I will be sharing with you the Jira token and Jira email ID for you. What you need to do is you need to create a simple application in the python which has a front -end, also you can use a simple front application in python. Create this into the python- you can use streamlit or whatever you want to use it is okay. The options will be in the front page there will be an option very similar to chatgpt where we will have where we can I can interact where I can enter the details for example I will ask you create a test cases for this Jira ID and I will basically click on the send. After that what will happen is that you will be able to fetch the detail there will be a second screen in this case where you will have a screen in this screen you will have the Jira email ID you can take the configuration of Jera email ID your email ID Url this and save it. Also in this case okay now what you need to do is it is a very simple application by using the olama- by using the olama local model which we are using which is gema 31 billion parameter one which is I have already installed in local, I want you to basically do 1 thing you need to use the Olama connection which is already running right now create this as it is. Now if user do not want to use Olama what you can do is make a fall back mechanism where you can use the groq.com - I will share the token also and token will be also available into the setting format. It is a simple  page application where user will ask you to create a test plan based on jra ticket you will fetch the Jira ticket automatically from the Jira and create a test case based on the template which is present into your template folder. 

# Now convert the above to a better prompt. Take the above and put this in Chatgpt/claude and ask it to produce a proper promt using RICEPOT method. Give a RICEPOT example for it to understand

# Once the proper prompt is formed store it in Prompt.md
# create a rough diagram of how you want the screens to look
# Now in command code enter plan mode. Ask the command code to use Prompt.md and the diagram to create a proper plan of the application and store it in plan.md.
# in command code enter agent mode and ask command code to start creating it as per the plan in src folder. Python files get created
# Create/make changes to.env file by providing JIRA, Groq token ids etc.
# once ready, ask command code to run the application and check the connectivity